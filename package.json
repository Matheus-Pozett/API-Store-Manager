{
  "name": "store-manager",
  "version": "2.0.0",
  "description": "Projeto Store Manager",
  "author": "Trybe",
  "license": "ISC",
  "main": "src/server.js",
  "engines": {
    "node": ">=16.14.0",
    "npm": ">=8.3.1"
  },
  "scripts": {
    "dev:local": "npm run dev --prefix backend",
    "test": "npm run test:mocha --prefix backend",
    "test:coverage": "npm run test:coverage --prefix backend",
    "test:mutation": "npm run test:mutation --prefix backend"
  },
  "dependencies": {
    "camelize": "^1.0.1"
  }
}
